let error = {
  room_join: "Can't join room, doesn't exist or you can't be identified.",
}

let event = {
  room_join: "has joined the room:",
  room_leave: "has left the room.",
}

module.exports.error = error;
module.exports.event = event;